<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ ticker }} Stock Advisory</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f5f5f5;
        }
        .container {
            background-color: white;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .header {
            border-bottom: 3px solid #007bff;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }
        .ticker-info {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        .ticker-name {
            font-size: 2.5em;
            font-weight: bold;
            color: #007bff;
        }
        .exchange {
            background-color: #28a745;
            color: white;
            padding: 5px 10px;
            border-radius: 15px;
            font-size: 0.9em;
        }
        .price-section {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 25px;
        }
        .current-price {
            font-size: 2em;
            font-weight: bold;
            color: #333;
        }
        .price-change {
            font-size: 1.2em;
            font-weight: bold;
        }
        .positive { color: #28a745; }
        .negative { color: #dc3545; }
        .neutral { color: #6c757d; }
        
        .advisory-section {
            background-color: #fff3cd;
            border-left: 5px solid #ffc107;
            padding: 20px;
            margin: 25px 0;
        }
        .action-badge {
            display: inline-block;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 0.9em;
        }
        .action-hold { background-color: #6c757d; color: white; }
        .action-add { background-color: #28a745; color: white; }
        .action-add_small { background-color: #20c997; color: white; }
        .action-trim { background-color: #fd7e14; color: white; }
        .action-take_profit { background-color: #17a2b8; color: white; }
        .action-reduce { background-color: #ffc107; color: black; }
        .action-exit { background-color: #dc3545; color: white; }
        
        .metrics-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin: 25px 0;
        }
        .metric-card {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            text-align: center;
        }
        .metric-value {
            font-size: 1.5em;
            font-weight: bold;
            color: #007bff;
        }
        .metric-label {
            font-size: 0.9em;
            color: #6c757d;
            margin-top: 5px;
        }
        
        .levels-table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        .levels-table th, .levels-table td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #dee2e6;
        }
        .levels-table th {
            background-color: #007bff;
            color: white;
        }
        
        .signals-list {
            list-style-type: none;
            padding: 0;
        }
        .signals-list li {
            background-color: #e7f3ff;
            margin: 8px 0;
            padding: 10px;
            border-radius: 5px;
            border-left: 4px solid #007bff;
        }
        
        .risk-alert {
            background-color: #f8d7da;
            border: 1px solid #f5c6cb;
            color: #721c24;
            padding: 15px;
            border-radius: 8px;
            margin: 20px 0;
        }
        
        .footer {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #dee2e6;
            font-size: 0.9em;
            color: #6c757d;
            text-align: center;
        }
        
        @media (max-width: 600px) {
            body { padding: 10px; }
            .container { padding: 20px; }
            .ticker-info { flex-direction: column; text-align: center; }
            .metrics-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="ticker-info">
                <div>
                    <div class="ticker-name">{{ ticker }}</div>
                    <div class="exchange">{{ exchange }}</div>
                </div>
                <div style="text-align: right;">
                    <div style="font-size: 0.9em; color: #6c757d;">{{ generated_at }}</div>
                </div>
            </div>
        </div>

        <div class="price-section">
            <div class="current-price">{{ "{:,.0f}".format(price) }} VND</div>
            <div class="price-change {{ 'positive' if change >= 0 else 'negative' }}">
                {{ "+" if change >= 0 else "" }}{{ "{:,.0f}".format(change) }} VND 
                ({{ "+" if change_pct >= 0 else "" }}{{ "{:.2f}".format(change_pct) }}%)
            </div>
        </div>

        <div class="advisory-section">
            <h3>🎯 Advisory Recommendation</h3>
            <div style="margin-bottom: 15px;">
                <span class="action-badge action-{{ advisory.action }}">{{ advisory.action.replace('_', ' ') }}</span>
            </div>
            <p><strong>Rationale:</strong> {{ advisory.rationale }}</p>
            
            {% if advisory.risk_notes %}
            <div class="risk-alert">
                <strong>⚠️ Risk Notes:</strong> {{ advisory.risk_notes }}
            </div>
            {% endif %}
        </div>

        <div class="metrics-grid">
            <div class="metric-card">
                <div class="metric-value {{ 'positive' if performance.pl_pct >= 0 else 'negative' }}">
                    {{ "+" if performance.pl_pct >= 0 else "" }}{{ "{:.2f}".format(performance.pl_pct) }}%
                </div>
                <div class="metric-label">P/L vs Avg Price</div>
            </div>
            <div class="metric-card">
                <div class="metric-value">{{ "{:.1f}".format(pct_to_target) }}%</div>
                <div class="metric-label">Distance to Target</div>
            </div>
            <div class="metric-card">
                <div class="metric-value">{{ "{:,.0f}".format(performance.total_value) }}</div>
                <div class="metric-label">Position Value (VND)</div>
            </div>
            <div class="metric-card">
                <div class="metric-value">{{ shares }}</div>
                <div class="metric-label">Shares Held</div>
            </div>
        </div>

        {% if technical and technical.rsi14 %}
        <h3>📊 Technical Analysis</h3>
        <div class="metrics-grid">
            <div class="metric-card">
                <div class="metric-value">{{ technical.rsi14 }}</div>
                <div class="metric-label">RSI (14)</div>
            </div>
            {% if technical.sma and technical.sma.trend %}
            <div class="metric-card">
                <div class="metric-value">{{ technical.sma.trend.replace('_', ' ').title() }}</div>
                <div class="metric-label">SMA Trend</div>
            </div>
            {% endif %}
            {% if technical.volume and technical.volume.volume_ratio %}
            <div class="metric-card">
                <div class="metric-value">{{ "{:.1f}".format(technical.volume.volume_ratio) }}x</div>
                <div class="metric-label">Volume vs 20D Avg</div>
            </div>
            {% endif %}
            {% if technical.macd and technical.macd.crossover %}
            <div class="metric-card">
                <div class="metric-value">{{ technical.macd.crossover.title() }}</div>
                <div class="metric-label">MACD Signal</div>
            </div>
            {% endif %}
        </div>
        {% endif %}

        {% if advisory.key_signals %}
        <h3>🔍 Key Signals</h3>
        <ul class="signals-list">
            {% for signal in advisory.key_signals %}
            <li>{{ signal }}</li>
            {% endfor %}
        </ul>
        {% endif %}

        <!-- Price Levels Section - Safe fallback -->
        {% if advisory and advisory.get('levels') %}
        <h3>💰 Price Levels</h3>
        <div class="info-box">
            {% if advisory.levels.get('add_zone') %}
            <p><strong>Add Zone:</strong> {{ advisory.levels.add_zone }}</p>
            {% endif %}
            {% if advisory.levels.get('take_profit_zone') %}
            <p><strong>Take Profit Zone:</strong> {{ advisory.levels.take_profit_zone }}</p>
            {% endif %}
            {% if advisory.levels.get('hard_stop') %}
            <p><strong>Hard Stop:</strong> {{ advisory.levels.hard_stop }}</p>
            {% endif %}
        </div>
        {% endif %}

        {% if advisory and advisory.get('next_checks') %}
        <h3>📋 Next Checks</h3>
        <ul class="signals-list">
            {% for check in advisory.next_checks %}
            <li>{{ check }}</li>
            {% endfor %}
        </ul>
        {% endif %}

        {% if fundamentals and fundamentals.pe_ratio %}
        <h3>📈 Fundamentals</h3>
        <div class="metrics-grid">
            <div class="metric-card">
                <div class="metric-value">{{ "{:.1f}".format(fundamentals.pe_ratio) }}</div>
                <div class="metric-label">P/E Ratio</div>
            </div>
            <div class="metric-card">
                <div class="metric-value">{{ "{:.1f}".format(fundamentals.dividend_yield) }}%</div>
                <div class="metric-label">Dividend Yield</div>
            </div>
            {% if fundamentals.sector != "Unknown" %}
            <div class="metric-card">
                <div class="metric-value">{{ fundamentals.sector }}</div>
                <div class="metric-label">Sector</div>
            </div>
            {% endif %}
        </div>
        {% endif %}

        <div class="footer">
            <p>This analysis is generated automatically and should not be considered as investment advice. 
               Please conduct your own research and consult with a financial advisor before making investment decisions.</p>
            <p><em>Generated by VN Stock Advisory Notifier</em></p>
        </div>
    </div>
</body>
</html>
        }
        .summary {
            margin-bottom: 20px;
        }
        .advisory {
            margin-top: 20px;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            background-color: #f9f9f9;
        }
        .levels {
            margin-top: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Stock Advisory for {{ ticker }}</h1>
        <div class="summary">
            <p><strong>Current Price:</strong> {{ price }}</p>
            <p><strong>Average Price:</strong> {{ avg_price }}</p>
            <p><strong>Target Price:</strong> {{ target_price }}</p>
            <p><strong>Shares Held:</strong> {{ shares }}</p>
            <p><strong>P/L Percentage:</strong> {{ pl_pct_vs_avg }}%</p>
        </div>
        <div class="advisory">
            <h2>Advisory</h2>
            <p><strong>Action:</strong> {{ action }}</p>
            <p><strong>Rationale:</strong> {{ rationale }}</p>
            <p><strong>Risk Notes:</strong> {{ risk_notes }}</p>
            <p><strong>Key Signals:</strong> {{ key_signals | join(', ') }}</p>
        </div>
        <div class="levels">
            <h2>Levels</h2>
            <table>
                <tr>
                    <th>Level</th>
                    <th>Price Range</th>
                </tr>
                <tr>
                    <td>Add Zone</td>
                    <td>{{ advisory.levels.add_zone | join(' - ') if advisory.levels and advisory.levels.add_zone else 'N/A' }}</td>
                </tr>
                <tr>
                    <td>Take Profit Zone</td>
                    <td>{{ advisory.levels.take_profit_zone | join(' - ') if advisory.levels and advisory.levels.take_profit_zone else 'N/A' }}</td>
                </tr>
                <tr>
                    <td>Hard Stop</td>
                    <td>{{ advisory.levels.hard_stop if advisory.levels and advisory.levels.hard_stop else 'N/A' }}</td>
                </tr>
            </table>
        </div>
        <div class="news">
            <h2>Recent News</h2>
            <ul>
                {% for news_item in news %}
                    <li>{{ news_item }}</li>
                {% endfor %}
            </ul>
        </div>
    </div>
</body>
</html>